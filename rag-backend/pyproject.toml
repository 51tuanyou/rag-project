[project]
name = "rag-backend"
version = "0.1.0"
description = "Default template for PDM package"
authors = [
    {name = "AlbertLiu", email = "huafeng.liu@xenera-corp.com"},
]
dependencies = ["django>=5.2.7"]
requires-python = ">=3.12"
readme = "README.md"
license = {text = "MIT"}

[build-system]
requires = ["pdm-backend"]
build-backend = "pdm.backend"


[tool.pdm]
distribution = true
